pipeline {
     agent {
          dockerContainer {
               image 'python-build-image'  // tu imagen con Python
               args '-u root:root'         // opcional, si necesitas permisos
          }
     }
     environment { VENV_DIR = "venv" }
    stages {
        stage('Info Inicial') { steps { echo "Pipeline iniciado" } }
        stage('Crear entorno virtual') { steps { sh 'python3 -m venv venv' } }
        stage('Instalar dependencias') {
            steps {
                sh '''
                source venv/bin/activate
                pip install --upgrade pip
                pip install -r requirements.txt
                '''
            }
        }

        stage('check') {
            steps {
                sh 'python3 --version'
            }
        }

        stage('Ejecutar script Python') {
            steps {
                sh '''
                python script.py
                '''
            }
        }
    }
    post {
        success { echo "Pipeline completado correctamente" }
        failure { echo "Pipeline falló, revisar logs" }
    }
}
